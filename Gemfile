source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in reportage.gemspec
gemspec

# PDF Generation
gem 'prawn'
gem 'axlsx'

group :development, :test do
  gem 'pry-byebug'
end
